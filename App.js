import React, {
  useEffect
} from 'react';
import {
  firebaseApp
} from './src/utils/firebase-connector';
import * as firebase from 'firebase'
import Navigation from './src/components/Navigation';

export default App = () => {

  return (< Navigation />);
}
